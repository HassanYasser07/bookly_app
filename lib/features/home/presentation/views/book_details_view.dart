import 'package:flutter/material.dart';
class BoolDetailsView extends StatelessWidget {
  const BoolDetailsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
